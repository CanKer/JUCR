name: CI

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  ci:
    runs-on: ubuntu-latest

    services:
      mongo:
        image: mongo:7
        ports:
          - 27017:27017
        options: >-
          --health-cmd "mongosh --eval 'db.runCommand({ ping: 1 })' || exit 1"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 20

    env:
      MONGO_URI: mongodb://localhost:27017/jucr
      OCM_API_KEY: ff82541f-c8d1-4507-be67-bd07e3259c4e
      OCM_BASE_URL: http://localhost:3999
      FAKE_OCM_PORT: 3999

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install
        run: npm install

      - name: Lint
        run: npm run lint

      - name: Typecheck
        run: npm run typecheck

      - name: Build
        run: npm run build

      - name: Unit tests
        run: npm run test:unit

      - name: Start fake OCM server
        run: |
          node dist/fake-ocm-server.js &
          sleep 1

      - name: E2E tests
        run: npm run test:e2e
